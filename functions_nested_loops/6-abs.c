#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @x: number to check
 * Return: x
 */
int _abs(int x)
{
	if (x < 0)
	{
		return (x * -1);
	}
	return (x);
}



